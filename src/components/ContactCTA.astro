---
import CallToAction from './CallToAction.astro';
import Icon from './Icon.astro';
---

<aside>	
	<h3>Voulez-vous que l'on travaille ensemble ?</h3>
	<div>
		<div class="buttons">
			<CallToAction href="mailto:thifagne.maxime@gmail.com">
				Envoyez-moi un message
			</CallToAction>	
		</div>
		<div class="buttons">
			<link href="https://assets.calendly.com/assets/external/widget.css" rel="stylesheet">
			<script src="https://assets.calendly.com/assets/external/widget.js" type="text/javascript" async></script>
			<a class="calendly" href="" onclick="Calendly.initPopupWidget({url: 'https://calendly.com/thifagne-maxime?hide_event_type_details=1&hide_gdpr_banner=1'});return false;">Réserver un appel</a>
		</div>
	</div>

</aside>

<style>
	aside {
		display: flex;
		flex-direction: column;
		align-items: center;
		gap: 3rem;
		border-top: 1px solid var(--gray-800);
		border-bottom: 1px solid var(--gray-800);
		padding: 5rem 1.5rem;
		background-color: var(--gray-999_40);
		box-shadow: var(--shadow-sm);
	}

	.buttons{
		margin: 1.5rem;
	}

	h2 {
		font-size: var(--text-xl);
		text-align: center;
		max-width: 15ch;
	}

	@media (min-width: 50em) {
		aside {
			padding: 5rem;
			flex-direction: row;
			flex-wrap: wrap;
			justify-content: space-between;
		}

		h2 {
			font-size: var(--text-3xl);
			text-align: left;
		}
	}

	a {
		position: relative;
		display: flex;
		place-content: center;
		text-align: center;
		padding: 0.56em 2em;
		gap: 0.8em;
		color: var(--accent-text-over);
		text-decoration: none;
		line-height: 1.1;
		border-radius: 999rem;
		overflow: hidden;
		background: var(--gradient-accent-orange);
		box-shadow: var(--shadow-md);
		white-space: nowrap;
	}

	@media (min-width: 20em) {
		.calendly {
			font-size: var(--text-lg);
		}
	}

	/* Overlay for hover effects. */
	.calendly::after {
		content: '';
		position: absolute;
		inset: 0;
		pointer-events: none;
		transition: background-color var(--theme-transition);
		mix-blend-mode: overlay;
	}

	.calendly:focus::after,
	.calendly:hover::after {
		background-color: hsla(var(--gray-999-basis), 0.3);
	}

	@media (min-width: 50em) {
		.calendly {
			padding: 1.125rem 2.5rem;
			font-size: var(--text-xl);
		}
	}
</style>
